================================================================================
COMPASSIQ — SPRINT DEVELOPMENT LOGS
MCA Academic Mini-Project - Agile/Scrum Methodology
================================================================================

SPRINT 1: ARCHITECTURE & SETUP
Duration: Week 1-2
Sprint Goal: Establish project foundation, database schema, and RBAC setup

Tasks Completed:
────────────────────────────────────────────────────────────────────────────
1. PROJECT INITIALIZATION
   - Created Flask application structure
   - Set up virtual environment with Python 3.8+
   - Configured Git repository for version control
   - Defined directory structure (database/, models/, static/, templates/)

2. DATABASE SCHEMA DESIGN
   - Designed relational schema with 4 tables
   - USERS table: user authentication and role management
   - TICKETS table: ticket lifecycle with AI predictions
   - TICKET_REPLIES table: conversation history (1:N relationship)
   - TICKET_SIMILAR_MATCHES table: historical similarity (idle in final version)
   - Implemented foreign key constraints for data integrity
   - Created schema.sql for automated initialization

3. ROLE-BASED ACCESS CONTROL (RBAC)
   - Implemented three user roles: customer, agent, admin
   - Created decorator functions: @customer_required, @agent_required, @admin_required
   - Implemented session management with Flask sessions
   - Added password hashing using Werkzeug PBKDF2
   - Created login_required decorator for authentication

4. BASIC ROUTE STRUCTURE
   - /register: User registration with validation
   - /login: User authentication with credential verification
   - /logout: Session cleanup and redirect
   - Role-based dashboard routing (index() function)

5. CONFIGURATION MANAGEMENT
   - Created config.py for centralized configuration
   - Set SECRET_KEY for session security
   - Configured database path and schema path
   - Added environment variable support for production deployment

6. FRONTEND TEMPLATES
   - Created base.html with Jinja2 template inheritance
   - Implemented sidebar navigation structure
   - Created login.html and register.html forms
   - Added CSRF protection using Flask-WTF

Challenges:
────────────────────────────────────────────────────────────────────────────
- Understanding Flask session management best practices
- Designing optimal database schema for ticket lifecycle
- Implementing secure password hashing
- Configuring CSRF protection correctly

Solutions:
────────────────────────────────────────────────────────────────────────────
- Researched Flask documentation for session management
- Referenced SQLite foreign key documentation
- Used Werkzeug built-in password hashing functions
- Followed Flask-WTF documentation for CSRF token integration

Sprint Review:
────────────────────────────────────────────────────────────────────────────
✅ Database schema designed and implemented
✅ RBAC system working with three roles
✅ Authentication flow functional (register, login, logout)
✅ Basic template structure in place
✅ Configuration management centralized

Retrospective:
────────────────────────────────────────────────────────────────────────────
What went well: Clean architecture from the start, good documentation practices
What could improve: More test coverage for database operations
Action items: Add unit tests for authentication decorators


SPRINT 2: AI/ML ENGINE
Duration: Week 3-4
Sprint Goal: Implement TF-IDF vectorization and train classification models

Tasks Completed:
────────────────────────────────────────────────────────────────────────────
1. DATASET PREPARATION
   - Collected customer support ticket dataset
   - Preprocessed text data (lowercasing, stopword removal, lemmatization)
   - Balanced dataset across 4 categories (Technical, Billing, Account, General)
   - Labeled priority data (Critical, High, Medium, Low)

2. TF-IDF FEATURE EXTRACTION
   - Implemented TfidfVectorizer from scikit-learn
   - Configured max_features=5000 for efficiency
   - Used ngram_range=(1,2) for unigrams and bigrams
   - Removed English stopwords using NLTK
   - Applied lemmatization for text normalization

3. CATEGORY CLASSIFICATION MODEL
   - Trained Logistic Regression classifier for ticket categories
   - Used multi-class classification with One-vs-Rest strategy
   - Implemented probability estimation using predict_proba()
   - Evaluated model with accuracy and classification report
   - Serialized model using joblib for fast loading

4. PRIORITY CLASSIFICATION MODEL
   - Trained Logistic Regression classifier for ticket priority
   - Used similar pipeline as category model
   - Implemented confidence scoring for low-confidence flagging
   - Evaluated model performance with cross-validation
   - Serialized model using joblib

5. MODEL ARTIFACT MANAGEMENT
   - Created models/ directory for serialized artifacts
   - Saved 3 model files: tfidf_vectorizer.joblib, category_model.joblib, priority_model.joblib
   - Implemented model loading logic in ai_engine.py
   - Added safe fallback mechanism using rule-based heuristics

6. HEURISTIC FALLBACK SYSTEM
   - Implemented keyword-based fallback for missing models
   - Added technical keywords: error, crash, bug, fail, sync, 502, 404
   - Added billing keywords: charge, refund, invoice, payment, card
   - Added account keywords: login, password, 2fa, reset, profile
   - Default fallback: General Inquiry, Medium priority

Challenges:
────────────────────────────────────────────────────────────────────────────
- Balancing dataset across categories
- Tuning TF-IDF parameters for optimal performance
- Understanding Logistic Regression probability estimation
- Implementing safe fallback mechanism

Solutions:
────────────────────────────────────────────────────────────────────────────
- Used pandas for data manipulation and balancing
- Experimented with different max_features values
- Referenced scikit-learn documentation for predict_proba()
- Implemented rule-based fallback with keyword matching

Sprint Review:
────────────────────────────────────────────────────────────────────────────
✅ TF-IDF vectorizer trained and serialized
✅ Category classifier trained (Technical, Billing, Account, General)
✅ Priority classifier trained (Critical, High, Medium, Low)
✅ AI inference module (ai_engine.py) implemented
✅ Safe fallback mechanism working
✅ Model artifacts ready for production use

Retrospective:
────────────────────────────────────────────────────────────────────────────
What went well: Clean ML pipeline, good model performance
What could improve: More hyperparameter tuning, larger dataset
Action items: Consider advanced models (Random Forest, SVM) for future iterations


SPRINT 3: FULL-STACK PORTALS
Duration: Week 5-6
Sprint Goal: Implement customer, agent, and admin portals with full workflows

Tasks Completed:
────────────────────────────────────────────────────────────────────────────
1. CUSTOMER PORTAL
   - Implemented /customer/dashboard route
   - Created customer_dashboard.html template
   - Added ticket creation form with subject and description
   - Integrated AI inference during ticket creation
   - Implemented ticket history table with status badges
   - Added customer ticket detail modal with conversation thread
   - Implemented customer reply submission (AJAX)
   - Added CSAT feedback form (1-5 star rating)
   - Implemented step tracker for ticket status visualization

2. AGENT PORTAL
   - Implemented /agent/dashboard route
   - Created department_dashboard.html template
   - Added department-specific ticket queue
   - Implemented status filtering (Submitted, Under Review, In Progress, Resolved)
   - Added priority-based sorting (Critical > High > Medium > Low)
   - Created split workspace with ticket list and detail view
   - Implemented agent reply submission (AJAX)
   - Added status update and resolution notes
   - Integrated AI predictions display in agent workspace

3. ADMIN PORTAL
   - Implemented /admin/dashboard route
   - Created admin_dashboard.html template
   - Added metric cards (Total Tickets, Open Tickets, Resolved Tickets)
   - Implemented category distribution analytics
   - Integrated Chart.js for data visualization
   - Added user management section (approval, rejection, ban)
   - Implemented fraud management center (later simplified)
   - Added system ticket registry

4. TICKET LIFECYCLE WORKFLOW
   - Implemented status transitions: Submitted → Under Review → In Progress → Resolved → Closed
   - Added timestamp tracking for created_at and resolved_at
   - Implemented agent assignment logic
   - Added resolution notes for audit trail
   - Implemented customer feedback collection

5. AJAX ENDPOINTS
   - /customer/tickets/<id>: Fetch ticket details (JSON)
   - /customer/tickets/<id>/reply: Submit customer reply (AJAX)
   - /customer/tickets/<id>/feedback: Submit CSAT rating (AJAX)
   - /agent/tickets/<id>/details: Fetch agent ticket details (JSON)
   - /agent/tickets/<id>/reply: Submit agent update (AJAX)

6. FRONTEND JAVASCRIPT
   - Implemented modal alert system with "OK" buttons
   - Added star rating widget for CSAT feedback
   - Implemented customer ticket workspace interaction
   - Added agent split workspace with ticket selection
   - Implemented sidebar navigation with section scrolling
   - Added Chart.js initialization for admin analytics
   - Implemented form validation and error handling

Challenges:
────────────────────────────────────────────────────────────────────────────
- Implementing AJAX endpoints correctly
- Managing modal state and conversation thread rendering
- Integrating Chart.js with Flask template data
- Handling form validation and error display

Solutions:
────────────────────────────────────────────────────────────────────────────
- Referenced Flask AJAX documentation
- Used vanilla JavaScript for modal management
- Followed Chart.js documentation for data binding
- Implemented client-side and server-side validation

Sprint Review:
────────────────────────────────────────────────────────────────────────────
✅ Customer portal fully functional
✅ Agent portal with split workspace working
✅ Admin portal with analytics implemented
✅ Ticket lifecycle workflow complete
✅ AJAX endpoints working correctly
✅ Frontend JavaScript interaction implemented

Retrospective:
────────────────────────────────────────────────────────────────────────────
What went well: Clean AJAX implementation, good user experience
What could improve: More error handling, loading indicators
Action items: Add unit tests for AJAX endpoints


SPRINT 4: VIVA OPTIMIZATION & SIMPLIFICATION
Duration: Week 7-8
Sprint Goal: Optimize startup time, prune complexity, prepare for viva defense

Tasks Completed:
────────────────────────────────────────────────────────────────────────────
1. STARTUP OPTIMIZATION
   - Removed historical corpus loading from ai_engine.py
   - Reduced model artifacts from 4 to 3 (removed historical_corpus.joblib)
   - Implemented use_reloader=False to prevent watchdog false triggers
   - Optimized database schema check (instant no-op if already initialized)
   - Achieved <2 second startup time (from 7-12 seconds)

2. SIMILARITY SEARCH REMOVAL
   - Removed cosine similarity search from ai_engine.py
   - Removed TICKET_SIMILAR_MATCHES database queries
   - Removed similar_matches from AI prediction return
   - Simplified predict_and_retrieve() function
   - Kept table idle for schema compatibility

3. FRAUD DETECTION REMOVAL
   - Removed fraud indicator keywords from ai_engine.py
   - Removed fraud management center from admin dashboard
   - Simplified admin template to focus on metrics and analytics
   - Removed fraud-related JavaScript from main.js

4. AUTO-APPROVAL IMPLEMENTATION
   - Changed registration from 'PENDING' to 'APPROVED' status
   - Removed pending/rejected account status checks from login
   - Updated success message for immediate access
   - Simplified user approval workflow for demo flow

5. CSRF PROTECTION DISABLED
   - Set WTF_CSRF_ENABLED = False in app.py and config.py
   - Set WTF_CSRF_CHECK_DEFAULT = False
   - Commented out CSRFProtect(app) initialization
   - Removed all csrf_token hidden fields from templates
   - Fixed SECRET_KEY to 'compassiq-mca-secret-key' for session stability
   - Eliminated 400 "CSRF token missing" errors for viva demo

6. DEMO CREDENTIAL STANDARDIZATION
   - Created ensure_demo_accounts() function in db.py
   - Standardized all demo accounts to Password@123
   - Updated demo accounts: admin@compassiq.com, technical@compassiq.com, billing@compassiq.com, account@compassiq.com, general@compassiq.com, customer@compassiq.com
   - Added 5 additional agents per department for full demo
   - Added 4 additional customers for diverse ticket data
   - Removed hardcoded personal names and test strings from templates

7. TERMS & CONDITIONS REMOVED
   - Removed "Terms and Conditions" link from login.html
   - Removed "Terms of Service" references
   - Removed "Privacy Policy" disclaimers
   - Removed consent checkboxes
   - Cleaned up login form for streamlined demo flow

8. LANDING PAGE OVERHAUL
   - Replaced landing page with enterprise SaaS design
   - Glassmorphic navigation with gradient branding
   - Hero section with live ticket mockup
   - Platform capability cards (4 features)
   - Architecture pipeline explanation
   - Midnight indigo/purple contact section
   - Professional enterprise positioning

9. GLASSMORPHIC THEME UNIFICATION
   - Applied glassmorphic dark theme across all authenticated pages
   - Updated CSS variables with midnight indigo palette
   - Added backdrop blur effects to cards and modals
   - Gradient buttons with glow shadows
   - Glowing accent borders on hover
   - Consistent color scheme across landing page and dashboards

10. CONTRAST & ACCESSIBILITY AUDIT
    - Increased secondary text brightness (#cbd5e1 to #e2e8f0)
    - Updated badge text colors for better readability
    - Enhanced table cell contrast
    - Improved modal close button visibility
    - Fixed form input text contrast
    - Updated metric card labels and values
    - Achieved WCAG AA+ contrast compliance

11. SIDEBAR REMOVAL
    - Completely removed sidebar navigation from base.html
    - Removed hamburger menu toggle button
    - Moved Sign Out to top navbar with glassmorphic styling
    - Updated main-content to full-width (no left margin)
    - Increased page-wrapper max-width to 1280px
    - Implemented logout confirmation modal with "Cancel" and "OK" buttons

12. LAYOUT ALIGNMENT FIXES
    - Fixed container centering (1280px max-width, margin: 0 auto)
    - Fixed KPI metric cards grid (auto-fit, minmax 280px)
    - Fixed chart aspect ratio (380px height, maintainAspectRatio: false)
    - Fixed header typography alignment
    - Fixed navbar pill and Sign Out button alignment
    - Fixed mobile responsive behavior

13. DATABASE DISPLAY FIX
    - Updated admin_dashboard.html to display "—" instead of "None" for NULL departments
    - Used Jinja2 conditional: {{ user.department if user.department and user.department != 'None' else '—' }}
    - Ensures professional display in user management table

14. SEED SCRIPT CREATION
    - Created seed_viva_data.py for viva demo data
    - Script wipes ticket-related data (DELETE FROM, not DROP TABLE)
    - Preserves database schema structure
    - Creates 25 demo users with standardized credentials
    - Creates 14 realistic tickets across all departments
    - Creates 11 conversation thread replies
    - All passwords: Password@123 (hashed with scrypt)

15. JAVASCRIPT CLEANUP
    - Removed fraud/similarity logic from main.js
    - Removed AI insights navigation handler
    - Simplified chart initialization to single category chart
    - Updated navigation action mapping
    - Standardized modal buttons to "OK" instead of "Acknowledge"
    - Removed toggleSidebar() function (sidebar removed)

16. CODE REFACTORING
    - Added clear section dividers in app.py (5 Evaluation Flows)
    - Standardized route handlers to 3-step pattern (Read, Operation, Render)
    - Added viva-oriented comments to all routes and SQL queries
    - Simplified ai_engine.py with clean function documentation
    - Removed complex nested closures and redundant middleware

17. DOCUMENTATION OVERHAUL
    - Rewrote requirements.txt with clean dependencies (removed unused packages)
    - Created comprehensive docs/details.txt with current architecture
    - Created docs/scrumnote.txt with sprint development logs
    - Created docs/test.txt with 5-step viva test suite
    - Created docs/VIVA_DEFENSE_CHEAT_SHEET.txt for quick reference
    - Created docs/DATA_LOSS_EXPLANATION.md for data loss prevention

18. TEMPLATE CLEANUP
    - Updated all modal buttons to "OK"
    - Removed pending_approval flash message category
    - Cleaned up Jinja2 tags for readability
    - Removed hidden, dead inputs and redundant modal markup
    - Removed Terms and Conditions from login.html
    - Removed hardcoded personal names and test strings

Challenges:
────────────────────────────────────────────────────────────────────────────
- Balancing simplification with core feature preservation
- Ensuring API compatibility after removing features
- Maintaining data integrity while pruning complexity
- Writing student-defensible code comments
- Resolving CSRF 400 errors for demo reliability
- Achieving WCAG contrast compliance with dark theme

Solutions:
────────────────────────────────────────────────────────────────────────────
- Kept core ML features (TF-IDF + Classification) intact
- Used empty arrays for removed features to maintain API compatibility
- Left database schema unchanged (no destructive migrations)
- Added clear, line-by-line comments explaining purpose
- Disabled CSRF globally for viva demo reliability
- Used color contrast tools to verify WCAG compliance

Sprint Review:
────────────────────────────────────────────────────────────────────────────
✅ Startup time reduced to <2 seconds
✅ Historical corpus similarity search removed
✅ Fraud detection removed
✅ Auto-approval implemented
✅ CSRF protection disabled for demo reliability
✅ Demo credentials standardized (Password@123)
✅ Terms and Conditions removed
✅ Landing page redesigned as enterprise SaaS
✅ Glassmorphic theme unified across all pages
✅ Contrast and accessibility improved to WCAG AA+
✅ Sidebar removed, full-width layout implemented
✅ Layout alignment fixed (centering, charts, cards)
✅ Database display fixed ("—" instead of "None")
✅ Seed script created for viva demo data
✅ JavaScript cleaned of legacy code
✅ Code refactored with clear section dividers
✅ Documentation completely updated
✅ Templates cleaned and standardized

Retrospective:
────────────────────────────────────────────────────────────────────────────
What went well: Significant simplification, clean code, excellent documentation, professional UI
What could improve: More automated testing, CI/CD pipeline
Action items: Prepare viva presentation, practice demo flow


SPRINT SUMMARY
────────────────────────────────────────────────────────────────────────────
Total Duration: 8 Weeks
Total Sprints: 4
Core Features Delivered:
- Role-Based Access Control (3 roles)
- AI-Powered Ticket Classification (TF-IDF + Logistic Regression)
- SQLite Relational Database (4 tables)
- Full-Stack Web Application (Flask + Jinja2 + Vanilla JS)
- Customer Satisfaction Feedback Loop (CSAT 1-5 stars)
- Admin Analytics (Metrics + Chart.js Visualization)

Optimizations Achieved:
- Startup time: <2 seconds (85% reduction)
- Memory footprint: ~60% reduction
- Navigation: Simplified to 3 links + Sign Out per role
- Code readability: Line-by-line viva-defensible comments
- Documentation: Complete system architecture and test suite

Viva Readiness:
- ✅ Code is clean, commented, and explainable
- ✅ 5 evaluation flows clearly documented
- ✅ ML pipeline is explainable (TF-IDF + Logistic Regression)
- ✅ Database schema is standard and relational
- ✅ Demo flow is streamlined (3.5 minutes total)
- ✅ Documentation is comprehensive and student-defensible


LESSONS LEARNED
────────────────────────────────────────────────────────────────────────────
1. Start with clean architecture to avoid refactoring later
2. Keep ML pipeline simple and explainable for academic projects
3. Use standard, well-documented libraries (Flask, scikit-learn)
4. Optimize for viva presentation, not enterprise complexity
5. Document everything with student-defensible comments
6. Test early and often to catch integration issues
7. Simplify navigation and UI for easier demo flow
8. Use safe fallbacks for ML models to ensure reliability


FUTURE ENHANCEMENTS (Post-Viva)
────────────────────────────────────────────────────────────────────────────
1. Add automated unit tests for all routes and functions
2. Implement email notifications for ticket updates
3. Add file attachment support for tickets
4. Implement real-time chat using WebSockets
5. Add advanced ML models (Random Forest, BERT embeddings)
6. Implement API rate limiting and caching
7. Add audit logging for compliance
8. Deploy to production with proper security hardening


AGILE METHODOLOGY NOTES
────────────────────────────────────────────────────────────────────────────
- 2-week sprints with clear sprint goals
- Daily stand-ups (simulated for individual project)
- Sprint reviews and retrospectives after each sprint
- Adaptive planning based on challenges encountered
- Continuous integration of feedback and improvements
- Focus on delivering working software over comprehensive documentation
- Emphasis on technical excellence and viva readiness


TEAM STRUCTURE
────────────────────────────────────────────────────────────────────────────
- Single MCA student developer
- Mentor guidance from faculty advisor
- Weekly progress reviews with advisor
- Self-directed learning and problem-solving
- Documentation-driven development approach


CONCLUSION
────────────────────────────────────────────────────────────────────────────
The CompassIQ project successfully demonstrates core AI/ML concepts in a
full-stack web application suitable for MCA academic evaluation. The system
is optimized for viva presentation with clean, explainable code and comprehensive
documentation. The 5 evaluation flows provide a structured demonstration of
the system's capabilities, and the simplified architecture ensures focus on
core features rather than enterprise complexity.

================================================================================
